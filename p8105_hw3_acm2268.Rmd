---
title: "p8105_hw3_acm2268"
author: "Amanda Miles"
date: "10/16/2021"
output: github_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(tidyverse)
library(readxl)
library(dplyr)
```

#Question 1

##Reading in data

```{r p1_reading_in_data}

library(p8105.datasets)
data("instacart")

```

```{r p1_understanding_data}

instacart_df = tibble(instacart$aisle_id, instacart$aisle) %>%
  sort(aisle_id, decreasing = TRUE)

```

This dataset is the Instacart Online Grocery Shopping Dataset from 2017 and the file is loaded from p8105.datasets. There are `r nrow(instacart)` rows of data and `r ncol(instacart)` variables in the dataset. The specific variables included are `r names(instacart)`. Additionally, my data is structured as `r typeof(instacart`.

There are `r max(instacart$aisle_id)` aisles. The most items are ordered from aisles `r names(sort(table(instacart$aisle_id), decreasing = TRUE)[1:3])`, which are the `r names

